language: ruby
rvm:
  - 2.2.5
env:
  global:
    - SOURCE_BRANCH=source
    - TARGET_BRANCH=target
before_script:
  - ./travis/clone
  - ./travis/decrypt
script:
  - touch output/foo
after_script:
  - ./travis/push
branches:
  only:
    - source
